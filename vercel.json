{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun run build && bun src/index.ts",
  "rewrites": [
    { "source": "/public/:match*", "destination": "/:match*" },
    { "source": "/(.*)", "destination": "/api" }
  ]
}